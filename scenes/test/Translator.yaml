# Command: yas scenes/test/Translator.yaml 

title: Test "Translator" extension
extensions:
  - ../../dist/src/Translator        # Register a new extension
steps:
  - Translator:
      text: hello
      lang: vi
      var: translatedText
      
  - Echo~green: Result is "${translatedText}"